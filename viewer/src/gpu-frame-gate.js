// SPDX-License-Identifier: Apache-2.0

//! The gpu-frame-gate module lives in the viewer package so hosts get the same code;
//! the application keeps this path for its own modules and tests.

export * from "../../bindings/viewer/src/gpu-frame-gate.js";
